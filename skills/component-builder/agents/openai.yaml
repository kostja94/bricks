interface:
  display_name: Component Builder
  short_description: Build new interface components in their real project context.
  default_prompt: Build the requested new component while preserving the project's design system, stack, and interaction conventions.
