# Footer construction example

```text
Use component-builder to replace the current footer.

Keep the existing framework and design tokens. The site is a bilingual SaaS product with product, resources, company, and legal destinations. Use real routes from the repository, preserve keyboard access, and verify the result on mobile and desktop.
```

Expected behavior: the agent inspects the project, reads the Footer reference, selects an appropriate grouped variant, implements it with existing primitives, and validates it in the actual page context.
